autoEdit
========

Extender for Burp Suite <http://portswigger.net/burp/> (only for Pro version).
This extention permits to edit automatically specific parameters with a pre-defined method.
Supported methods :
- base64 encode
- base64 decode
- url (double) encode
- url (double) decode

-- Burp Extender API (all Java files, except BurpExtender.java) :  <http://portswigger.net/burp/extender/api/burp_extender_api.zip>
-- JavaDoc for Bur Extender API : <http://portswigger.net/burp/extender/api/index.html>

ToDo
====

Adding methods :
- md5
- sha1
- XOR (with user-defined key)

Features :
- multi-methods selection
- user-defined methods

Compilation
===========

The script clean burp/*.class files, then compiles the sources and creates a JAR file.

$ chmod +x make.sh
$ ./make.sh
added manifest
adding: burp/BurpExtender$1.class(in = 4320) (out= 2064)(deflated 52%)
adding: burp/BurpExtender.class(in = 7295) (out= 3197)(deflated 56%)

The JAR archive only contains BurpExtender*.class, the others classes are part of the API and are not necessary.

Usage
=====

You can directly use the JAR file autoEdit.jar in you Burp Suite.
